<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- The fill color for the button -->
    <solid android:color="@color/ips_blue" />
    <!-- Rounded corners; adjust the radius as desired -->
    <corners android:radius="8dp" />
    <!-- Optional padding within the button -->
    <padding
        android:left="6dp"
        android:top="6dp"
        android:right="6dp"
        android:bottom="6dp" />
</shape>
